# embedded_money_management
A basic embedded Java application that manages a digital store, handles product inventory, and provides a simple HTTP-based API with JSON responses. Built using core Java, no external frameworks.
